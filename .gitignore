# Build
build/
.xmake/
Data/sfse/

# Packaging
dist/
*.zip

# Papyrus compilation temp
temp_scripts/

# LikeC4 rendered output (CI renders fresh for GitHub Pages)
docs/dist/

# Local RE workspace (Ghidra project DB, champollion binary — use re/ for checked-in artifacts)
/tools/
/ghidra-project/

# Large reference file — download separately from the Address Library release
offsets-*.txt

# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.vs/
.vscode/settings.json
.idea/
*.user

# IntelliSense — paths are machine-specific, regenerate via `xmake project -k compile_commands .`
compile_commands.json

# Claude Code transient state (keep skills/ committed)
.claude/scheduled_tasks.lock
.claude/*.json

.fake